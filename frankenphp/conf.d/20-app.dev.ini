; Pick up source-code changes on every request (no worker mode in dev)
opcache.enable = 1
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0

; Longer timeout for slow cache rebuilds (dev container compilation can be slow on Windows Docker)
max_execution_time = 120
